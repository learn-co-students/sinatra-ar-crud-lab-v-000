<!-- We need to create an erb file in the views directory, new.erb, 
with a form that POSTs to a controller action, /posts. The controller 
action should use the Create CRUD action to create the blog post and 
save it to the database. Then, the action uses erb to render the index view page. -->

<form method="POST" action="/posts">
  <h1>Create a New Blog Post</h1>
  <p>Post Name:<input type="text" name="name"></p>
  <p>Post Content:<input type="textarea" name="content"></p>
  <input type="submit" id="submit">
</form>