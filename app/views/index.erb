<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Posts</title>
  </head>
  <body>
    <h2>Your Newest Post</h2>

    <h4><%=Post.newest_post.name%></h4>
    <p><%=Post.newest_post.content%></p>

    <h2>Your Older Posts</h2>

    <% Post.all_other_posts.each do |p| %>
    <h4><%= p.name %></h4>
    <p><%= p.content %></p>
    <% end %>

  </body>
</html>
