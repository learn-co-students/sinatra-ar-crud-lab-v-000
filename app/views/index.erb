<!DOCTYPE html>
<html>

  <head>
    <title>Posts</title>
  </head>

  <body>

    <% @posts.each do |post| %>
      <h3>Title: <%= post.name%></h3>
      <p>Content: <%= post.content %></p>
    <% end %>

  </body>
</html>

<!-- Post.all.each do |post|
post.name
post.content
end -->
