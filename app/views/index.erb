<h1>Blog Index</h1>
<% @posts.each_with_index do |post, index| %>
  <h2>Blog Post <%= index + 1%></h2>
  <p>
    Post Name: <%= post.name %>
  </p>
  <p>
    Post Content: <%= post.content %>
  </p>
<% end %>
