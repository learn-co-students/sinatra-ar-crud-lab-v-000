<h1>All posts</h1>
<ul>
  <% @posts.each do |post| %>
  <li>
    <h4><a href="/posts/<%= post.id %>"><%= post.name %></a></h4>
    <p>
      <%= post.content %>
    </p>
  </li>
  <% end %>
</ul>
<a href="/posts/new">Add Post</a>
