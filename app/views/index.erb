<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>HOME</title>
  </head>
  <body>

    <ul>
      <% @posts.each do |post| %>
        <li>
          <a href="/posts/<%= post.id %>"><%= post.name %></a>
          <p><%= post.content %></p>
        </li>
      <% end %>
    </ul>

  </body>
</html>
