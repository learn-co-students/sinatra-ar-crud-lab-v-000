<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>View All Posts</title>
  </head>
  <body>
    <h2>POSTS</h2>
    <% Post.all.each do |post| %>
      <h3>
        <%= "#{post.name}" %>
      </h3>
      <p>
        <%= "#{post.content}" %>
      </p>
    <% end %>
  </body>
</html>
