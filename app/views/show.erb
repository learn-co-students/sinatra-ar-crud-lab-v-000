<!DOCTYPE html>
<html>
  <%= @article.title %>
  <%= @article.content %>
  <!-- Delete Article  -->
  <form method="POST" action="/articles/<%= @article.id %>">
    
    <input type="hidden" name="_method" value="DELETE">
    <input type="submit">
    
  </form>
</html>