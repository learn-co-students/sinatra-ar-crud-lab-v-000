<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Blog Post No. <%= @post.id %></title>
  </head>
  <body>

    <h2><%= @post.name %></h2>

    <p><%= @post.content %></p>

    <form method="POST" action="/posts/<%= @post.id %>/delete">
      <input id="hidden" type="hidden" name="_method" value="delete">
      <input type="submit" value="delete">
    </form>

  </body>
</html>
