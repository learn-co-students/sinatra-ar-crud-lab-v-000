<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <a href="/posts/<%=@post.id %>"><%= @post.name %><%= @post.content %>></a>
    <h1><%= @post.name %></h1>
    <h2><%= @post.content %></h2>

    <form action="/posts/<%= @post.id %>/delete" method="post">
      <input type="hidden" id="hidden" name="_method" value="delete">
      <input type="submit" value="delete">
    </form>
    <h2><%= @post.name %> was deleted</h2>
  </body>
</html>
