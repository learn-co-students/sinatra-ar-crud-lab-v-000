<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Sinatra Blogging App</title>
  </head>
  <body>
    <h1>Hey, I'm the edit page for Post# <%= @post.id %>!</h1>
    <p>Current Name: <%= @post.name %></p>
    <p>Current Content: <%= @post.content %></p>
    <h3>_____________________________</h3>
    <form method="POST" action="/posts/<%= @post.id %>">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <p>Post Name: <input type="text" name="name"></p>
      <p>Post Content: <input type="text" name="content"></p>
      <input type="submit" name="submit" value="submit">
    </form>
  </body>
  </html>