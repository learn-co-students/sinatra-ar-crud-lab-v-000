<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit</title>
  </head>
  <body>
    <form action="/posts/<%= @post.id %>" method="post">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <p>Post Name: <input type="text" name="name" value="<%= @post.name %>"></p><br>
      <p>Post Content: <textarea name="content" rows="10" cols="60"><%= @post.content %></textarea></p><br>
      <input type="submit" value="submit">
    </form>
  </body>
</html>
