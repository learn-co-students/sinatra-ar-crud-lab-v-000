<!DOCTYPE html>
<html>
  <head>
    <title>Update</title>
  </head>
  <body>
    <h3>Current Post</h3>
    <p><%= @post.name %></p>
    <p><%= @post.content %></p>
    
    <h3>Update Post</h3>
    
    <form method="post" action="/posts/<%= @post.id %>">
      <input id="hidden" type="hidden" name="_method" value="patch"></p>
    <p><label> Post Name: </label>
      <input type="text" name="name" value="<%= @post.name %>">
    <p><label> Post Content: </label>
      <input type="text" name="content" value="<%= @post.content %>">

    <input type="submit" value="submit">
    </form>
  </body>
</html>