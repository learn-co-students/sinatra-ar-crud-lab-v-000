#Placeholder for a model

#after creating our db in sqlite by running in terminal "rake db:migrate_creation NAME=create_posts"
#we need to setup our class Post that can receive the ActiveRecord::Base module methods

class Post < ActiveRecord::Base
end
